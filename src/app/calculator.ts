namespace AppTest 
{
    export const calculator = {
        add: function(x: number, y: number) {
            return x + y;
        },
        sub: function(x: number, y: number) {
            return x - y;
        }
    };
}